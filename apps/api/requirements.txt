# Pinned: every install gets the same versions (upgrade deliberately, not by
# accident of install date). Versions match the working dev venv.

# Core API (always needed)
fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic-settings==2.11.0
yt-dlp==2025.10.14
# Lets modern `X | None` type syntax work on Python 3.9 (prod uses 3.11+).
eval_type_backport==0.3.1

# Local transcription engine (dev default). Cloud engines are optional:
#   pip install openai        # for TRANSCRIBE_ENGINE=openai
#   pip install assemblyai    # for TRANSCRIBE_ENGINE=assemblyai
faster-whisper==1.2.1

# Database
sqlalchemy==2.0.50
psycopg2-binary==2.9.12
alembic==1.16.5
